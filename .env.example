# Supabase project (browser client)
VITE_SUPABASE_URL=https://PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: override the dev API target. Defaults to http://localhost:3000.
# In production, /api/* is handled by the Vercel rewrite in vercel.json.
VITE_DEV_API_URL=http://localhost:3000
